version: '3'
services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENV_VAR=production
    volumes:
      - .:/app
    depends_on:
      - db

  db:
    image: postgres:13
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mydatabase
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:  
    